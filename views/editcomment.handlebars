 <form id="new-comment-form" class="card">
    {{!-- <div class="card-header">
      <h3>Add New Comment</h3>
    </div> --}}

    <div class="card-body">
      <input type="hidden" name="post-id" value="{{comment.post_id}}" />

      <label class="form-label" for="comment-body">Edit comment</label>

      <textarea name="comment-body" class="form-input" id="comment-desc">{{comment.description}}</textarea>

      <button type="submit" class="btn save-btn">Save</button>
    </div>
  </form>
</div>
</section>